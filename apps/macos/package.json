{
  "name": "@maridlabsai/jini-macos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run prepare:sidecar && npm run validate && tauri dev",
    "build": "npm run prepare:sidecar && npm run validate && tauri build",
    "web:dev": "vite --host 127.0.0.1",
    "web:build": "vite build",
    "prepare:sidecar": "node scripts/prepare-sidecar.mjs",
    "validate": "node scripts/validate-shell.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^6.0.0"
  }
}
